version: "3"
services:
  mongodb:
    image: mongo:4.2.5-bionic
    ports:
      - 27017:27017
  backend:
    image: git_python_test:0.2.1
    ports:
      - 9000:9000
  loadbalancer:
    image: nginx
    ports:
      - 80:80
    volumes:
    - ./loadbalancer/nginx.conf:/etc/nginx/nginx.conf