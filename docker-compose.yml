version: "3"
services:
  mongodb:
    image: mongo:4.2.5-bionic
    ports:
    - 27017:27017
  app:
    build: /app
    ports:
    - 9000:9000
  proxy:
    image: nginx:1.17.9-alpine
    ports:
    - 80:80
    volumes:
    - ./proxy/nginx.conf:/etc/nginx/nginx.conf